# @sphere/graph

In-memory Sphere graph projection helpers.

This package replays verified event chains into queryable entity and edge maps. The initial projection supports:

- `entity.create`
- `edge.create`
- `getEntity(id)`
- `getEdge(id)`
- `getEdgesFrom(sourceId)`
- `getEdgesTo(targetId)`
